// ส่วนนี้แทรกในฟอร์มบันทึกยอดขายรายวัน
<div className="border rounded-lg p-4 bg-slate-50 mt-4">
  <h3 className="font-bold text-lg mb-4 text-blue-700">🕒 ข้อมูลแรงงาน (Labor & Hours)</h3>
  
  <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
    {/* Input 1: ชั่วโมงจริง */}
    <div>
      <Label>Actual Hours (PT+FT)</Label>
      <Input 
        type="number" 
        value={formData.actualHours} 
        onChange={e => handleChange('actualHours', e.target.value)}
        placeholder="ไม่รวม Duty Team"
        className="bg-white"
      />
    </div>

    {/* Input 2: OT */}
    <div>
      <Label>OT Hours</Label>
      <Input 
        type="number" 
        value={formData.otHours} 
        onChange={e => handleChange('otHours', e.target.value)}
        className="bg-white"
      />
    </div>

    {/* Input 3: จำนวนคนปิดร้าน */}
    <div>
      <Label>คนปิดร้าน (รับค่ารถ)</Label>
      <Input 
        type="number" 
        value={formData.closeShiftCount} 
        onChange={e => handleChange('closeShiftCount', e.target.value)}
        className="bg-white"
      />
    </div>
  </div>

  {/* ส่วนแสดงผลลัพธ์การคำนวณอัตโนมัติ (Read Only) */}
  <div className="grid grid-cols-2 md:grid-cols-4 gap-2 bg-white p-3 rounded border">
    
    <div className="text-center p-2 bg-gray-100 rounded">
      <div className="text-xs text-gray-500">Summary Hours</div>
      <div className="font-bold text-lg">{calculated.summaryHours?.toFixed(1) || '-'}</div>
      <div className="text-[10px] text-gray-400">Duty+Actual+OT</div>
    </div>

    <div className={`text-center p-2 rounded ${calculated.varianceHours > 0 ? 'bg-red-50 text-red-600' : 'bg-green-50 text-green-600'}`}>
      <div className="text-xs">Variance Hours</div>
      <div className="font-bold text-lg">{calculated.varianceHours?.toFixed(1) || '-'}</div>
      <div className="text-[10px]">เทียบ Roster</div>
    </div>

    <div className="text-center p-2 bg-yellow-50 rounded border-yellow-100">
      <div className="text-xs text-yellow-700">COL %</div>
      <div className="font-bold text-xl text-yellow-800">{calculated.colPercent?.toFixed(2) || '0.00'}%</div>
    </div>

    <div className="text-center p-2 bg-blue-50 rounded border-blue-100">
      <div className="text-xs text-blue-700">TCMH</div>
      <div className="font-bold text-xl text-blue-800">{calculated.tcmh?.toFixed(2) || '0.00'}</div>
    </div>

  </div>
</div>